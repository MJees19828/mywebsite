import React from "react"

function Home(){
  return (
    <section>
    <h1>Who are we?</h1>
    <p>Specificity is the means by which browsers decide which  CSS property values are the most relevant to an 
     an element and, therefore will be applied. Specifiity is based on the matching rules which are conposed of different sorts of CSS selectors</p>
       <button> READ MORE</button>
         </section>
  )

}

export default Home